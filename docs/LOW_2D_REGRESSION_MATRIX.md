# Matriz de regresión de LOW 2D

Casos derivados de problemas reales comunicados por usuarios de software de
animación. Esta matriz es un contrato de producto: un control no se considera
terminado hasta modificar el modelo real, participar de Undo/Redo, guardarse y
tener una comprobación proporcional al riesgo.

## P0 — integridad y recuperación

| ID | Escenario de aceptación | Tipo | Estado |
| --- | --- | --- | --- |
| SAVE-01 | Modificar un Drawing, `Ctrl+S`, cerrar y reabrir: contenido idéntico. | Modelo/integración | Automatizado |
| SAVE-02 | Modificar varios Levels, exposiciones, paleta, cámara y onion skin: un único guardado recupera todo. | Modelo/integración | Automatizado |
| SAVE-03 | Si falla la escritura, la versión guardada anterior permanece intacta y el documento sigue dirty. | Integración host/filesystem | Automatizado (`check_save_recovery_ui.js`) |
| SAVE-04 | El mensaje de éxito sólo aparece después de confirmar la escritura completa. | Interacción | Automatizado (`check_save_recovery_ui.js`) |
| HIST-01 | Cada comando destructivo publica nombres concretos para Undo y Redo. | Unidad | Automatizado |
| HIST-02 | Copy/cut/paste desde Xsheet y Timeline ejecutan los mismos comandos y producen el mismo estado. | Modelo/interacción | Automatizado (comando único `shortcuts.cells`) |
| HIST-03 | Una transacción compuesta se deshace en un solo paso y en orden inverso. | Unidad | Automatizado por HistoryManager |
| RECV-01 | Un checkpoint conserva ruta, contenido, hora y última operación. | Unidad | Automatizado |
| RECV-02 | Tras cierre inesperado se ofrecen Recover, Discard y Compare sin cargar silenciosamente el archivo. | E2E | Automatizado (`check_save_recovery_ui.js`) |
| RECV-03 | Descartar una recuperación no modifica el documento guardado ni vuelve a ofrecerla. | Unidad/E2E | Unidad automatizada |
| CRASH-01 | Un crash report incluye versión, OS, GPU/render, escena y último comando sin contenido privado innecesario. | Integración | Implementado con lista blanca en los dos lados del puente |
| LEVEL-01 | Crear un Level exige o propone un nombre descriptivo y conserva un ID interno estable. | Interacción/modelo | Automatizado (modelo) + propuesta de nombre y renombrado en la UI |
| STYLE-01 | Un estilo usado no puede borrarse sin reasignación. | Modelo | Automatizado |
| STYLE-02 | Reasignar y borrar forma una operación reversible y no deja referencias huérfanas. | Modelo | Automatizado (`replaceStyle`, una transacción) |

## P1 — estados que deben explicarse solos

| ID | Escenario de aceptación | Resultado esperado |
| --- | --- | --- |
| VIEW-01 | Frame actual sin exposición | Explica Level y frame; ofrece exposición anterior, crear Drawing o exponer el actual. |
| VIEW-02 | Drawing referenciado pero inexistente | Muestra número, Level y rango de frames afectado. |
| CAM-01 | Viewer con arte y Camera Output vacío | Diagnóstico por visibilidad, cobertura de cámara y conexión de output. |
| CAM-02 | Cambiar Drawing/Camera/Final Output | El modo activo queda rotulado inequívocamente. |
| ROOM-01 | Cerrar o mover todos los paneles y ejecutar Reset Current Room | Recupera el preset sin cambiar escena, frame, selección, onion ni History. |
| ROOM-02 | Reset All Rooms | Borra sólo layouts personalizados; nunca datos del proyecto. |
| AUDIO-01 | Importar WAV/MP3/OGG compatible | Muestra nombre, duración, frecuencia, canales y estado Ready. |
| AUDIO-02 | Audio válido sin sonido porque está muted | Diagnóstico `Playback is muted` con acción Unmute. |
| AUDIO-03 | Codec no compatible | Mensaje específico; no `Error` genérico. |
| ALPHA-01 | Exportar JPG con transparencia | Advierte que JPEG no admite alpha y ofrece PNG o fondo explícito. |
| PAL-01 | Cambiar de Level | Level, Palette y Style activos se actualizan juntos y quedan visibles. |
| SHORT-01 | Ejecutar comando por menú, toolbar, shortcut o contexto | Todas las rutas llaman al mismo Command Registry. |

## P2/P3 — crecimiento profesional

| ID | Escenario de aceptación | Prioridad |
| --- | --- | --- |
| BRUSH-01 | Crear, renombrar, agrupar, buscar y compartir un BrushPreset real. | P2 |
| BRUSH-02 | Cada parámetro visible produce una diferencia medible en el trazo. | P1 — **cerrado en v4.21.0**; automatizado (`check_brush_params_ui`). En raster los nueve cambian el trazo; en vectorial siete, y los dos que no —Presión → opacidad y Dureza— están **apagados con su motivo**, porque una cinta rellena no puede variar opacidad a lo largo ni difuminar el borde. |
| SAFE-01 | Inicio seguro usa workspace, shortcuts y brushes por defecto; no carga plugins ni proyecto previo. | P1 — automatizado en modelo, host y Chromium (`--safe-mode`) |
| SAFE-02 | Reset UI/Shortcuts/Brushes/2D actúa por dominio y permite cancelar. | P1 — automatizado; preserva escenas y recuperación |
| XS-COL | La hoja de exposición tiene las columnas que §6 pide: niveles, cámara, audio y efectos. | P2 — **automatizado en v4.26.0** (`check_xsheet_columnas_ui`). El audio se lee con `peakAt`, que aplica el desplazamiento de la pista; la aserción está verificada contra su violación. |
| NODE-01 | Editar puntos de un trazado, polígono o línea: mover, borrar, y un solo paso de historial por gesto. | P1 — **automatizado en v4.22.0** (`check_vector_nodes_ui`): formato del trazado (relativo, H/V, L implícita), mover sin deformar el resto, piso contra trazados degenerados, el arranque no se pierde, y **un gesto = un Ctrl+Z**. |
| INI-01 | LOW abre en el estudio 2D: la invitación ofrece crear o abrir, sus botones **responden al clic de verdad**, y se va —sin taparlo— en cuanto hay un documento abierto por cualquier camino. | P0 — **automatizado en v4.29.0** (`check_pantalla_inicial_ui`, 4 contratos). Dos defectos de v4.28.0 reportados por Mauro y cerrados acá: la invitación vive dentro del lienzo y sin estar en `DZ_UI_SEL` el `preventDefault()` del dibujo se comía el click; y `hayTrabajoAbierto()` miraba sólo `DZ.path`/`DZ.doc`, que con un diseño `.svg` abierto están **los dos en null**. Las dos aserciones verificadas contra su violación. |
| INI-02 | El estudio 2D se ve **al arrancar**, no cuando termina de cargar el lado de la IA. | P0 — **automatizado en v4.30.0** (`check_pantalla_inicial_ui` se instrumenta antes de navegar y compara el instante en que se pinta contra el de la llamada del arranque; 4 contratos). Medido en la app real: aparecía a los **6.542 ms**, porque la llamada está al final de `init()`, detrás de `api.get_state()`, `loadChatTabs()` y `resume()` — el dibujo esperando al chat. Ahora hay una fase temprana colgada de `DOMContentLoaded`, con las acciones apagadas hasta que existe el puente. Verificado mordiendo con el código anterior. |
| NAV-01 | Desde el estudio 2D se puede llegar al estudio 3D y volver, y ninguna pantalla queda tapando otra. | P1 — **automatizado en v4.32.0** (`check_arranque_recorrido_ui`, A02 del plan maestro; 2 contratos). La única puerta al 3D era `#abL3d` en la barra izquierda, y esa barra queda **tapada** por `#designView` (`position:fixed; inset:0`) — medido con `elementFromPoint`—, así que desde que el 2D es la primera pantalla el 3D quedó sin entrada. Y `#l3dView` está en z-index 62 contra 61 del 2D: mostrar el 2D sin cerrar el 3D dejaba al dibujante pidiendo volver al dibujo y mirando otra pantalla. Las dos aserciones verificadas contra su violación, con clics físicos (`Input.dispatchMouseEvent`). |
| DATOS-01 | Lo que la interfaz guarda —rescate ante caída, pinceles, disposición de paneles, espacio de trabajo— **sobrevive al cierre**. | P0 — **automatizado en v4.32.0** (4 contratos sobre `main.py`). Era **pérdida de datos en cada arranque**: pywebview 6.x trae `private_mode=True` (perfil efímero) y `http_port=None` (puerto al azar, y `localStorage` es por origen), y `main.py` no fijaba ninguno de los dos. Reproducido con clave testigo: escrita en `:17446`, y tras cerrar y reabrir `{testigo: null, claves: 0}` en `:61150`. Arreglado con `private_mode=False`, `storage_path` propio y `LOW_UI_PORT=47141` comprobado antes de arrancar. Verificado: el testigo sobrevive a un cierre forzado. |
| DATOS-02 | Tras un cierre forzado, la primera pantalla **ofrece recuperar** el trabajo sin guardar, diciendo de qué archivo es y de cuándo. | P0 — **automatizado en v4.32.0** (`check_pantalla_inicial_ui` siembra un punto de rescate; 3 contratos). El ofrecimiento vive dentro de `dzDocInit`, que sólo corre al crear o abrir un documento: desde que LOW abre **sin** documento, quien volvía de una caída veía un estudio vacío y ninguna señal de que su trabajo estaba guardado. **Hay dos almacenes** y el que sirve es el del documento (`LOW.workspace.recovery`, checkpoint cada 450 ms); el de la escena se escribe en momentos más gruesos y medido traía el lienzo vacío. Probado de punta a punta en la app real: trazo, cierre forzado, aceptar, y vuelve el trazo idéntico. |
| PAPEL-01 | «Mover documento a la papelera» pide confirmación, **cancelar no mueve nada**, y el puente mueve a `.low-trash` sin borrar jamás. | P0 — **automatizado en v4.32.0** (`check_papelera_ui` + `check_papelera_backend`). Lo encontró el inventario de A01: es la **única acción destructiva** del módulo 2D y no tenía guard, ni contrato, ni prueba de puente. Se prueban las cuatro promesas del puente —mueve y no borra, rechaza rutas de afuera del proyecto, no pisa nombres repetidos, no inventa archivos— y el flujo, cuya aserción central es que cancelar **no llame al puente**: si se llamara antes de confirmar, el «¿estás seguro?» sería decorado. Cinco aserciones verificadas contra su violación. |
| FORMA-01 | Una forma nace **sólo con contorno** —trazo y grosor actuales, sin relleno— y hay un interruptor para pedirla rellena. | P1 — **automatizado en v4.31.0** (`check_shape_tool_ui`, 2 contratos). Antes salía un bloque rojo macizo con `fill` y ningún `stroke`, que en una mesa de animación es al revés de lo que se necesita. Verificado mordiendo con el código anterior. |
| FORMA-02 | Al contorno de una forma se le puede poner **un pincel** como línea, y la forma guarda su geometría, su pincel y su grosor. | P1 — **automatizado en v4.31.0** (`check_shape_tool_ui`, 3 contratos). Los hijos del grupo son un render; el dato es `data-d`. El pincel va por id porque si al re-dibujar se leyera el actual, deformar la forma le cambiaría el trazo. Falta producción: un dibujo entintado de verdad con esto. |
| FORMA-03 | Una forma se **deforma libremente** con una jaula de puntos: la jaula queda guardada, se repone entera o de a un punto, la densidad se cambia sin perder lo hecho, y un tirón es **un** Ctrl+Z. | P1 — **automatizado en v4.31.0** (`check_warp_cage_ui`, 6 contratos). Usa `rigMalla` —la misma malla que dobla los dibujos del rig— y `dzDeformarElemento`. Con el contorno entintado, el pincel se rehace sobre la curva nueva: es lo que hace que FORMA-01/02/03 compongan. Falta producción: una escena animada con deformación. |
| PUNT-01 | Con el **encuadre de cámara a la vista** y una herramienta de dibujo elegida, un trazo dentro del cuadro **deja el trazo** y la cámara **no se mueve**; en modo cámara, arrastrar el encuadre sí la mueve. | P0 — **automatizado en v4.34.0** (`check_camara_encuadre_ui`, 2 contratos). Medido: el encuadre es un div transparente de 1011×568 px con `cursor:move` y sin `pointer-events` propio, así que con la cámara puesta **no entraba ningún trazo en toda la mesa** y cada intento **corría el plano** —que además deja clave en el cuadro—. Las dos mitades se prueban juntas porque apagar el encuadre entero arregla una y rompe la otra. Tres mutaciones verificadas. |
| PUNT-02 | Las herramientas que trabajan **sobre el dibujo** (bomba, plancha, pinza, imán, inflador, balde, cuentagotas) actúan sobre un trazo **seleccionado**: los tiradores de la caja de selección no se comen el puntero. | P1 — **automatizado en v4.34.0** (`check_drawing_workflow_ui`, 2 contratos). Salió de un fallo ajeno que decía «Bomba no engrosa pincel»: el tirador tapaba la línea, apretar ahí **redimensionaba** en vez de bombear y la herramienta pedía «acercate más a una línea» estando justo encima. `dzPickStroke` además mira ahora **a través** de la UI flotante. |
| AYU-01 | La ayuda al pasar el puntero alcanza **también el cajón** de herramientas secundarias. | P2 — **automatizado en v4.34.0** (`check_drawing_workflow_ui`, 1 contrato). El cajón se cuelga del `body` y no de `#designView`, así que las únicas herramientas sin ayuda eran justo las que nadie conoce de memoria. |
| EXP-01 | Un **cuadro en blanco** en el medio del rango **no desaparece** del export: salen tantos cuadros como pide el rango, y el hueco sale con el papel. | P0 — **automatizado en v4.35.0** (`check_export_anim_ui` + 4 contratos). Medido antes: cinco cuadros con el tercero vacío → el puente recibía **cuatro** PNG y la app informaba «4 cuadros (F1 a F5)», contradiciéndose sola. No es un cuadro de menos: todo lo que sigue al hueco **se adelanta un cuadro**. Y un hueco transparente lo rellena el codec de negro en MP4 y GIF. Tres mutaciones verificadas. |
| EXP-02 | Si se pierde un cuadro de verdad (falla la rasterización, o no se puede leer el archivo en el camino viejo), el aviso **lo dice**. | P1 — **automatizado en v4.35.0** (`check_export_anim_ui`, 1 contrato). Se inyecta una falla de rasterización a propósito. Antes salía informado como si todo hubiera ido bien y el que exporta se enteraba en el montaje. |
| EXP-03 | La **secuencia PNG** sale intacta, con los nombres **ordenables** y sin cuadros de la toma anterior; MP4 y GIF salen de verdad o el error **nombra la alternativa** que funciona. | P0 — **automatizado en v4.35.0** (`check_export_anim_backend`, 2 contratos). Dos defectos medidos: el relleno de ceros era fijo en 3, así que con 1000 cuadros —42 s a 24 fps— `_1000.png` se ordenaba **antes** de `_999.png`; y exportar 1000 cuadros y después 10 dejaba **990 cuadros viejos** en la carpeta, que el importador se llevaba mezclados. Cuatro mutaciones verificadas. |
| DOC-01 | «Nuevo documento» crea un archivo **`.low`** en `disenos/` **antes** de mostrar nada, el documento apunta a él y **Ctrl+S guarda ahí sin diálogo**; no se siembra ningún SVG suelto. | P0 — **automatizado en v4.36.0** (`check_escena_nueva_ui` + `check_escena_nueva_backend`, 6 contratos). Lo pidió Mauro después de medir su workspace: **157 dibujos sueltos —138 en blanco, de 323 bytes— y CERO `.low`**. La escena vivía en memoria hasta que alguien se acordara de guardar. Siete mutaciones verificadas, y comprobado en la **app real**: el `.low` aparece en el disco con el trazo adentro. |
| DOC-02 | Abrir un `.low` **muestra el dibujo** y **no lo vacía**. | P0 — **automatizado en v4.36.0** (`check_escena_nueva_ui`, 2 contratos). Era pérdida de datos: dentro de `#dzCanvas` hay otros `<svg>` hijos —rotoscopía, esqueleto, malla— que van **antes** en el HTML, y el editor busca el dibujo con `:scope > svg`. Sin haber abierto antes un `.svg` —justo lo que ofrece la primera pantalla— el dibujo se escribía en la hoja de rotoscopía, oculta, no se veía nada, y el primer `dzDocCommit()` **escribía el vacío encima del documento**. Trazado: `dzCanvasSet` con 145 caracteres y `ok:true`, y el commit siguiente con `dibujoAntes:145` y `lienzo:0`. |
| CHAT-01 | El menú «Copiar» del chat abre con el foco adentro y **cerrarse no tira ninguna excepción**. | P1 — **automatizado en v4.36.1** (`check_menu_contextual_ui`, 1 contrato). Salió del **`low.log` de Mauro**: «Uncaught TypeError: Cannot read properties of null (reading 'querySelector') @app.js:276», con su reporte en `fallos/`. El menú se cierra solo —`click`, `scroll` en captura y `blur`, y el chat se autodesplaza al llegar una respuesta— y el `requestAnimationFrame` leía la variable ya puesta en null. Reproducido con el mismo mensaje antes de arreglar. Las dos mitades se prueban juntas: sacar el `requestAnimationFrame` pasaría la prueba del cierre y rompería el teclado. |
| EXP-04 | La carpeta de Premiere sale desde el menú: **avisa antes** si no hay audio, **cancelar no escribe nada**, salen todos los cuadros del rango, y el **XML nombra archivos que existen**. | P0 — **automatizado en v4.37.0** (`check_export_premiere_ui` + `check_export_premiere_backend`, 2 contratos). Cerró el último «parcial» del inventario. Y destapó un defecto: el nombre de los cuadros lo calculan **dos lados** —el frontend lo escribe dentro del XML, el puente escribe los archivos— y tenían reglas de limpieza distintas; con una escena «El Gato (final)» el XML pedía `El_Gato_final__0001.png` y el puente escribía `El_Gato_final_0001.png`: Premiere abre eso con **todos los cuadros perdidos**, pidiendo relinkear uno por uno. Seis mutaciones verificadas. |
| LOG-01 | El log de diagnóstico de tableta **tiene tope** y al recortar conserva **lo último**. | P2 — **automatizado en v4.37.0** (`check_diag_log_backend`, 1 contrato). Medido en la máquina de Mauro: `diag-log.txt` con **23 MB** creciendo desde agosto dentro de `%APPDATA%`, porque `save_tablet_log` sólo hacía `append`. Recortar guardando el principio sería peor que no recortar: se tiraría justo lo que sirve para diagnosticar. Tres mutaciones verificadas. |
| FORMA-04 | **Achicar o deformar una forma no cambia el espesor de su contorno**, en ninguno de los lados. | P0 — **automatizado en v4.38.0** (`check_escala_contorno_ui`, 4 contratos). Lo reportó Mauro con la v4.37.0 puesta. Redimensionar escribía una `transform`, y en SVG eso escala también el trazo; con la deformación libre el factor es distinto por eje, así que los lados verticales se pintaban con el factor X y los horizontales con el Y. Medido antes: **0,32 px contra 4,52 px** en la misma forma. Ahora se escala la GEOMETRÍA —`x/y/width/height`, las coordenadas del trazado, o el `data-d` de una forma entintada, que se vuelve a entintar con el mismo `data-grosor`— y el `stroke-width` no se toca nunca. Tres mutaciones verificadas y comprobado en la app real. |
| INST-01 | Una **segunda instancia de LOW abre**, con LOW ya abierto. | P1 — **automatizado en v4.38.0** (`check_perfil_instancias_backend`, 1 contrato). Lo introdujo el arreglo de v4.32.0: fijar el perfil evitó perder pinceles y rescate, pero WebView2 lo toma **en exclusiva**, así que la segunda instancia moría con `0x8007139F` y una traza de .NET en el log —sin ventana ni aviso—. Apareció al intentar abrir LOW mientras Mauro lo tenía abierto. Ahora la segunda usa `webview-2` y queda dicho en el log. Dos mutaciones verificadas. |
| VEC-01 | Se pueden **agregar puntos** a una línea ya dibujada, y agregarlos **no la deforma**. | P0 — **automatizado en v4.39.0** (`check_herramientas_vector_ui`, 2 contratos). Lo pidió Mauro. Midiendo apareció la causa común de que «las herramientas de vector no funcionen»: una curva recién dibujada tiene **dos anclas, las dos en las puntas**, y todas las herramientas de vector trabajan sobre anclas. Con la herramienta de nodos, el primer clic elige la línea y el segundo agrega un punto, partiendo el tramo con De Casteljau para que la curva quede idéntica. Cuatro mutaciones verificadas. |
| VEC-02 | El **inflador infla la línea**, no la forma: sobre un trazo de pincel engorda **donde pasás** y con **Alt desinfla**. | P1 — **automatizado en v4.39.0** (`check_herramientas_vector_ui`, 2 contratos). Antes escalaba la geometría desde el centro, como un globo, y el grosor del contorno no lo tocaba. Ahora, como el Pump de OpenToonz: en un trazo de pincel sube la presión de los puntos bajo el cursor con caída suave —medido 1 → 2,03 en el medio con las puntas intactas— y en un trazo común, que tiene un solo grosor, lo dice el aviso. |
| VEC-03 | El **imán deforma** apoyado en el medio de una curva, donde no hay ancla. | P1 — **automatizado en v4.39.0** (`check_herramientas_vector_ui`, 1 contrato). Antes no movía **nada** ahí —sólo tiraba de anclas existentes— y el aviso decía «Deformación aplicada». Ahora agrega el punto que falta y tira de ése. |
| WS-01 | Las **pestañas de espacios de trabajo** están en la barra después de «Nuevo documento» y de abrir un `.low`, con una marcada como activa. | P0 — **automatizado en v4.40.0** (`check_taller_vector_ui`, 1 contrato). Regresión propia de v4.36.0: el flujo nuevo de documento no pasa por `openDesign`, que era quien las montaba. Mauro las llamó «fundamental». |
| VEC-04 | El inflador arranca **por tramo** —engorda el pedazo donde pasás— y «toda la línea» queda como **opción** de la herramienta. | P1 — **automatizado en v4.40.0** (`check_taller_vector_ui` + `check_herramientas_vector_ui`, 2 contratos). Un trazo de lápiz tiene UN grosor, así que al inflar por tramo la línea pasa a ancho variable —mismos puntos, mismo color, mismo grosor de partida— y desde ahí el tramo engorda solo. |
| VEC-05 | Una **forma básica** (rectángulo, círculo, elipse) se puede editar por puntos: pasa a trazado al tocarla con la herramienta de nodos, conservando el contorno, y se avisa. | P1 — **automatizado en v4.40.0** (`check_taller_vector_ui`, 1 contrato). Antes el editor decía «ese elemento no tiene nodos editables» y ahí se terminaba: no se podía dibujar a partir de formas básicas. |
| CAM-02 | La **zona de cámara** se marca en pantalla mientras se dibuja —cuadro, zona segura y marcas de esquina— y **no se come el puntero**; se puede apagar desde el menú. | P1 — **automatizado en v4.40.0** (`check_taller_vector_ui`, 1 contrato). Antes sólo se veía dentro del modo cámara: no saber qué entra en el plano se paga al exportar. Referencias: hojas de layout de Ghibli y storyboards con cuadro exterior y zona segura. |
| RIG-01 | Un modo contextual explica por qué un Level no admite mesh/bones y cómo prepararlo. | P3 |
| MOCAP-01 | Cancelar el análisis conserva íntegramente las siluetas anteriores y el estado del video. | P1 — automatizado en modelo/contrato |
| MOCAP-02 | Un punto sólo se completa entre dos observaciones confirmadas; nunca se extrapola fuera de ellas. | P1 — automatizado |
| MOCAP-03 | Reducir claves conserva extremos y cambios que superan la tolerancia elegida. | P1 — automatizado |
| MOCAP-07 | Una máscara válida produce geometría SVG visible; una máscara vacía no crea un cuadro blanco. | P0 — automatizado en modelo y Chromium |
| MOCAP-04 | Aplicar una toma al rig forma una sola transacción y no cambia pivotes ni jerarquía. | P1 — automatizado |
| MOCAP-05 | Un video real con oclusiones, paneo y dos sujetos produce diagnóstico comprensible y corrección manual. | P1 — **parcial**: validado con video real de UN sujeto sin oclusiones ni paneo. Faltan los tres casos difíciles |
| MOCAP-06 | El filtro elimina ruido aislado, mantiene continuidad de identidad y marca ausencia del sujeto como oclusión. | P1 — automatizado |
| MOCAP-13 | Fondo elegido, validación y navegación por problemas persisten y son reversibles con Ctrl+Z. | P1 — modelo/UI automatizados |
| MOCAP-08 | Los 33 hitos MediaPipe se traducen a 13 articulaciones LOW respetando región, confianza y lados. | P0 — automatizado |
| MOCAP-09 | Reanalizar reemplaza detecciones automáticas pero conserva cuadros corregidos manualmente. | P0 — automatizado |
| MOCAP-10 | Pose Landmarker ejecuta inferencia en un worker local y el hilo visual conserva un fallback seguro. | P1 — E2E Chromium |
| MOCAP-11 | Los apoyos se agrupan por pie e intervalo; Pies firmes estabiliza el contacto sin deformar la pose relativa. | P1 — modelo/UI automatizados |
| MOCAP-12 | La máscara semántica se proyecta desde la región del sujeto al cuadro completo sin borrar correcciones manuales. | P1 — **aceptado** (v4.14.0, video real: 67 siluetas y nivel de calco creado) |

## Límites conocidos y medidos

- **Resolución de la silueta: 192 px de ancho como máximo** (`localSilhouetteEngine`,
  `width = min(192, …)`). En una escena de 1920 cada píxel de máscara se agranda diez
  veces, y por eso el nivel de calco se ve a manchones. Es un techo elegido para que el
  análisis de decenas de cuadros no congele la interfaz, no un defecto del filtro.
- Con **line art** como entrada, la diferencia contra el fondo sólo se dispara donde hay
  tinta: el resultado son los trazos, no una silueta rellena. Es lo correcto para calcar,
  pero conviene no esperar una mancha sólida como con video de acción real.

## Regla para nuevos tests

Cada incidencia debe expresarse con una secuencia reproducible:

```text
estado inicial → acción → estado observable → guardar/cerrar/reabrir → estado final
```

Los tests de modelo cubren invariantes y serialización; los de integración
cubren el puente Python/pywebview y el filesystem; los E2E cubren mensajes,
botones de recuperación y flujos multiventana. Las pruebas visuales sólo se
usan para layout, contraste, selección y estados activos: nunca sustituyen una
afirmación sobre el modelo.
