/**
 * Paleta de marca de LOW para el estudio 3D.
 *
 * Son los mismos tokens que usa el resto de la app en `ui/app.css`
 * ("paleta de marca: negro / naranja / cyan"). El módulo 3D venía con el azul
 * de Windows (#0078d4) heredado del scaffold, que no es identidad de LOW.
 *
 * @module design/theme
 */

/** Naranja-rojo de LOW: acento principal (herramienta activa, selección). */
export const LOW_ACCENT = '#F0450E';
/** Variante oscura del acento, para fondos de fila seleccionada. */
export const LOW_ACCENT_DEEP = '#B8330A';
/** Cyan de LOW: acento secundario (guías, elementos de apoyo). */
export const LOW_CYAN = '#33B5E8';
