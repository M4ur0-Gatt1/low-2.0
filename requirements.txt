requests>=2.31
pywebview>=5.0
pillow>=10.0
vtracer>=0.6         # raster → SVG editable (vectorizar / "calcar"); offline
jsonschema>=4.21     # módulo social: valida la salida JSON del agente
cryptography>=42.0   # módulo social: tokens OAuth cifrados (Fernet)
keyring>=24.0        # módulo social: clave Fernet en el llavero del SO
# ffmpeg (binario, no pip) habilita export a MP4 y video; opcional
# Linux necesita ademas un backend web: pip install "pywebview[qt]"
# Para compilar: pip install pyinstaller
